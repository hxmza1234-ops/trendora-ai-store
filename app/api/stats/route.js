import { NextResponse } from 'next/server'; import { getState } from '../../../lib/store'; import { amazonConfigured } from '../../../lib/amazon'; import { shopifyConfigured } from '../../../lib/shopify'; export async function GET(){const s=await getState();return NextResponse.json({products:s.products.length,lastRefresh:s.updatedAt,refreshes:s.refreshes,amazon:amazonConfigured(),shopify:shopifyConfigured()})}
